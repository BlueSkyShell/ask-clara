# TESTNET-ONLY throwaway seed. Never put real funds on this.
# Generate one: node -e "import('viem/accounts').then(m=>console.log(m.generateMnemonic(m.english)))"
CLARA_SEED="replace with twelve words"
# Optional RPC override
# CLARA_RPC="https://ethereum-sepolia-rpc.publicnode.com"
