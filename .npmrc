# Electron Forge (desktop/) packages the app's node_modules and cannot follow
# pnpm's default symlinked layout, so use a hoisted (flat) node_modules.
# Standard requirement for Electron + pnpm projects.
node-linker=hoisted
