{
  "name": "ask-clara",
  "private": true,
  "packageManager": "pnpm@11.22.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "pnpm -r --if-present test",
    "check": "pnpm -r --if-present check"
  }
}